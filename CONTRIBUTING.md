# Contributing to mtbi_meeg

## Setting up local development environment
TBA: instructions to clone (Or fork), set up a conda env and install requirements

# Running tests
TBD, smth like
```bash
pytest tests/
```

## Submit changes 
Work on a separate branch, etc